const userValidation = require('./user.validation');
const purchaseValidation = require('./purchase.validation')
const cashbackValidation = require('./cashback.validation');

module.exports = {
    userValidation,
    purchaseValidation,
    cashbackValidation
}

